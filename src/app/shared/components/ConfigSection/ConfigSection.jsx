import React from "react";
import { Box } from "@mui/material";
import FilterSection from "@components/FilterSection";

const ConfigSection = ({ sx }) => {
  return (
    <Box sx={sx}>
      <FilterSection />
    </Box>
  );
};

export default ConfigSection;
